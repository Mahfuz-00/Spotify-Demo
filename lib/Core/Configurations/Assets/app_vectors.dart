class AppVectors {
  static const String BasedPath = 'Assets/Vectors/';
  static const String Format = '.svg';

  static const String Logo = '${BasedPath}spotify_logo${Format}';
  static const String Moon = '${BasedPath}moon${Format}';
  static const String Sun = '${BasedPath}sun${Format}';
  static const String TopPattern = '${BasedPath}top_pattern${Format}';
  static const String BottomPattern = '${BasedPath}bottom_pattern${Format}';
}